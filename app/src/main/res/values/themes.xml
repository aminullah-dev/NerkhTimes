<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.NerkhTimes" parent="@android:style/Theme.DeviceDefault.NoActionBar" />

    <!-- Android 12+ style splash, backward compatible via core-splashscreen.
         Shown instantly on cold start, hands off to Theme.NerkhTimes. -->
    <style name="Theme.NerkhTimes.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.NerkhTimes</item>
    </style>
</resources>
